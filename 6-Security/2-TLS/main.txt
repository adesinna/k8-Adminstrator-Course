Tls certificates are used for authentication.

A certificate is used to guarantee trust.

When you generate ssh

ssh-keygen

It generates two pairs of keys, private and public key.

We first generate pub and private key on the server

openssl genrsa -out name.key 1024

openssl rsa -in name.key -pubout > mybank.pem

